version: "3.8"

services:
  ansieyes:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./private-key.pem:/app/private-key.pem:ro
    restart: unless-stopped
